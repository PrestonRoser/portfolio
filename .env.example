# The site doesn't need any build-time variables right now.
#
# Don't put GITHUB_TOKEN here. It's a Worker secret, and the build fails on
# purpose if it's set. For `wrangler dev`, see .dev.vars.example.
