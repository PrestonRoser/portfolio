# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
*.log

# environment
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example
.wrangler/

# editors / OS
.DS_Store
.idea/
*.swp
*.tsbuildinfo

# Downloaded or generated before dev and build. Satoshi's license doesn't allow
# redistributing the font files, so they're never committed.
public/fonts/satoshi/
src/data/github-snapshot.json

# Local notes and tooling that aren't part of the site
docs/
design/
run.md
